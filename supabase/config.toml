project_id = "quelfdpqvzgnnvpuwljq"

[api]
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
port = 54323

[inbucket]
port = 54324

[storage]
file_size_limit = "50MiB"

[auth]
site_url = "https://tischtennis-schweinfurt.vercel.app"
additional_redirect_urls = []
jwt_expiry = 3600
enable_signup = true

# send-welcome-email wird ausschließlich durch den DB-Trigger via pg_net aufgerufen.
# pg_net sendet keinen JWT — verify_jwt = false ist zwingend erforderlich.
# Zugriffsschutz erfolgt über x-webhook-secret im Handler selbst.
[functions.send-welcome-email]
verify_jwt = false
